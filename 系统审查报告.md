# 物资管理系统 - 全面审查报告

> 审查时间：2026-04-01
> 审查范围：所有页面按钮交互、接口数据同步、提示信息显示

---

## 🔴 严重问题（影响核心功能）

### 问题 1：审批通过时库存扣减逻辑有缺陷
- **页面**：`ApprovalManagement.tsx` → `handleApprove` 函数
- **现象**：审批日常领用时，总是传 `sizeId: null`，即只扣总库存，**不会扣减对应规格的库存**
- **根因**：代码没有解析 `itemName` 中的规格信息（如 `(70g)`）来匹配 `sizes` 数组中的 `size_id`
- **文件位置**：`ApprovalManagement.tsx` 第 365 行
```tsx
await requestMaterialOut(
  targetItem.id,
  null, // ← 始终为 null，不会扣减具体规格库存
  requestQuantity,
  approval.id,
  ...
);
```
- **影响**：多规格物品（如A4打印纸70g/80g/90g）审批后只改了总库存，各个规格的 `sizes[].stock` 没有变化
- **修复方案**：从 `approval.itemName` 或 `approval.sizeLabel` 中解析规格ID，传给 `requestMaterialOut`

### 问题 2：物品上架时图片没有上传到数据库
- **页面**：`ItemUpload.tsx` → `handleUpload` 函数
- **现象**：用户选择图片后只有本地预览，但 `addMaterial()` 调用时**没有传入 `image_url`**，图片数据丢失
- **文件位置**：`ItemUpload.tsx` 第 410-419 行
```tsx
const newId = await addMaterial({
  ...
  // ← 没有 image_url: previewImage
});
```
- **影响**：上架的物品永远不会显示图片
- **修复方案**：需要先将图片上传到 Supabase Storage，获取 URL 后传入 `image_url` 字段

### 问题 3：物品申购时图片同样没有上传
- **页面**：`ItemPurchase.tsx` → `handleSubmit` 函数
- **现象**：用户选择图片后只有本地 DataURL 预览，但提交到 `requisitions` 表时**没有传 `image` 字段**
- **文件位置**：`ItemPurchase.tsx` 第 180-198 行
```tsx
await supabase.from('requisitions').insert({
  ...
  // ← 没有 image: selectedImage
});
```
- **影响**：申购单的图片在详情页看不到

---

## 🟡 中等问题（功能异常但非核心）

### 问题 4：删除合并物品时只会删除第一个匹配的记录
- **页面**：`ItemPermission.tsx` → `handleDelete` 函数
- **现象**：同名物品被合并显示为一个卡片，但"删除整个物品"只传了一个 `item.id`，只删除数据库中的第一条记录。如果同名物品有多条独立记录，其他记录仍然存在
- **文件位置**：`ItemPermission.tsx` 第 323-332 行
- **影响**：删除后刷新页面，物品可能仍然显示
- **修复方案**：获取合并物品的所有 `sourceIds`，逐个删除

### 问题 5：Dashboard 日期是硬编码的
- **页面**：`Dashboard.tsx` 第 156 行
- **现象**：右上角始终显示 `2026年3月18日`，不是当天实际日期
```tsx
<p className="text-sm font-semibold text-foreground">2026年3月18日</p>
```
- **影响**：用户体验不好
- **修复方案**：改为 `new Date().toLocaleDateString('zh-CN')`

### 问题 6：低库存预警页面最后更新时间也是硬编码的
- **页面**：`LowStockAlert.tsx` 第 1160 行
```tsx
<p className="text-xs text-muted-foreground">最后更新：2026-03-18 10:30</p>
```

### 问题 7：审批管理页面缺少批量审批/批量驳回功能
- **页面**：`ApprovalManagement.tsx`
- **现象**：表格头虽然有序号列，但没有复选框，管理员无法同时处理多条申请
- **影响**：当有大量待审核申请时，需要逐个点击审批，效率低

### 问题 8：分页功能是假的
- **页面**：`ApprovalManagement.tsx`、`ApplicationRecords.tsx`
- **现象**：显示"上一页/下一页"按钮，但始终是 `disabled` 状态，所有数据一次性全部加载显示
- **影响**：数据量大时页面性能下降
- **修复方案**：实现真正的分页逻辑，或改为无限滚动加载

---

## 🟢 轻微问题（体验优化）

### 问题 9：物品上架必填项检查引用了不存在的字段
- **页面**：`ItemUpload.tsx` 第 923 行
- **现象**：必填项检查列表中有 `stockPlatform: '组件库存台'`，但 `EMPTY_FORM` 中没有这个字段，永远显示为 ❌ 未完成
```tsx
{ key: 'stockPlatform', label: '组件库存台', filled: !!formData.stockPlatform },
```
- **影响**：表单永远显示"完成度"不足100%
- **修复方案**：删除这一项

### 问题 10：最近上架列表是硬编码的假数据
- **页面**：`ItemUpload.tsx` 第 973-977 行
- **现象**：右侧"最近上架"显示的数据是固定的示例数据（订书机3月17日、A4打印纸3月15日），不是实际数据
- **修复方案**：从 `allMaterials` 中取最近上架的物品

### 问题 11：搜索物资的查询语法可能有兼容问题
- **文件**：`materialsDB.ts` → `searchMaterials` 函数（第 388-392 行）
- **现象**：Supabase 的 `.or()` 方法中使用 `.ilike` 拼接可能存在SQL注入风险和语法问题
```tsx
.or(`category.ilike.%${query}%`)
```
- **修复方案**：使用 Supabase 推荐的 `textSearch` 或正确的 filter 语法

### 问题 12：`updateApplicationStatus` 使用了不推荐的 `.filter()` 方法
- **文件**：`applicationStore.ts` 第 137 行
- **现象**：使用 `.filter('id', 'eq', id)` 代替 `.eq('id', id)`，可能在某些 Supabase 版本中有兼容问题
- **修复方案**：统一改为 `.eq('id', id)`

### 问题 13：ManagementPlatform 领用排行数据来源可能为空
- **页面**：`ManagementPlatform.tsx`、`Dashboard.tsx`
- **现象**：领用排行查询的是 `requisitions` 表中 `requisition_type = 'daily_request'` 的记录，但日常领用申请实际写入的是 `application_records` 表，不是 `requisitions` 表
- **影响**：领用排行可能始终为空
- **修复方案**：改为查询 `application_records` 表中 `status = 'approved'` 的记录

---

## 📋 数据库交互正常的功能（无问题）

| 功能 | 页面 | 数据表 | 状态 |
|------|------|--------|------|
| 物品上架 | ItemUpload.tsx | materials | ✅ 数据正常写入 |
| 初始库存流水 | materialsDB.ts → addMaterial | inventory_logs | ✅ 正常记录 |
| 补货操作 | ItemPermission.tsx | materials (RPC函数) | ✅ 正常扣减 |
| 补货流水 | materialsDB.ts → restockMaterial | inventory_logs | ✅ 正常记录 |
| 删除物品 | ItemPermission.tsx | materials | ✅ 正常删除 |
| 删除规格 | ItemPermission.tsx | materials | ✅ 正常更新 |
| 日常领用申请 | DailyCollection.tsx | application_records | ✅ 正常写入 |
| 申购申请 | ItemPurchase.tsx | requisitions | ✅ 正常写入 |
| 审批状态更新 | ApprovalManagement.tsx | application_records | ⚠️ 数据写入正常，但库存扣减有缺陷 |
| 申购审批 | PurchaseManagement.tsx | requisitions | ✅ 正常更新 |
| 到货通知 | PurchaseManagement.tsx | requisitions | ✅ 正常更新 |
| 确认收货 | ApplicationRecords.tsx | requisitions | ✅ 正常归档 |
| 取消申购 | ApplicationRecords.tsx | requisitions | ✅ 正常更新 |
| 删除申请 | ApplicationRecords.tsx | application_records | ✅ 正常删除 |
| 导出Excel | 多个页面 | 客户端生成 | ✅ 正常导出 |
| 低库存预警 | LowStockAlert.tsx | materials | ✅ 正常读取 |

---

## 🔧 建议优先修复顺序

1. **🔴 问题 1** - 审批时多规格库存扣减（核心业务逻辑）
2. **🔴 问题 2** - 上架图片未上传（数据丢失）
3. **🔴 问题 3** - 申购图片未上传（数据丢失）
4. **🟡 问题 4** - 删除合并物品不完整
5. **🟡 问题 13** - 领用排行数据为空
6. **🟢 问题 9** - 必填项检查错误
7. **🟢 问题 5/6** - 日期硬编码
8. **🟢 问题 10** - 最近上架假数据
