# 物资管理系统 - 全面审查报告

> 审查时间：2026-04-01  
> 修复时间：2026-04-01  
> 审查范围：所有页面按钮交互、接口数据同步、提示信息显示

---

## ✅ 修复完成汇总（共 13 个问题，全部已修复）

| # | 问题 | 严重度 | 修复状态 | 修改文件 |
|---|------|--------|----------|----------|
| 1 | 审批通过时多规格库存不扣减 | 🔴 严重 | ✅ 已修复 | ApprovalManagement.tsx |
| 2 | 上架图片没有上传到数据库 | 🔴 严重 | ✅ 已修复 | ItemUpload.tsx, imageUpload.ts(新增) |
| 3 | 申购图片没有上传到数据库 | 🔴 严重 | ✅ 已修复 | ItemPurchase.tsx |
| 4 | 删除合并物品只删第一条记录 | 🟡 中等 | ✅ 已修复 | ItemPermission.tsx |
| 5 | Dashboard 日期硬编码 | 🟡 中等 | ✅ 已修复 | Dashboard.tsx |
| 6 | 低库存预警时间硬编码 | 🟡 中等 | ✅ 已修复 | LowStockAlert.tsx |
| 7 | 审批缺少批量操作 | 🟡 中等 | ✅ 已修复 | ApprovalManagement.tsx |
| 8 | 分页功能是假的 | 🟡 中等 | ✅ 已修复 | ApprovalManagement.tsx |
| 9 | 必填项检查引用不存在字段 | 🟢 轻微 | ✅ 已修复 | ItemUpload.tsx |
| 10 | 最近上架列表是假数据 | 🟢 轻微 | ✅ 已修复 | ItemUpload.tsx |
| 11 | 搜索查询语法兼容问题 | 🟢 轻微 | ✅ 已修复 | materialsDB.ts |
| 12 | updateApplicationStatus filter方法 | 🟢 轻微 | ✅ 已修复 | applicationStore.ts |
| 13 | 领用排行数据来源为空 | 🟢 轻微 | ✅ 已修复 | Dashboard.tsx, ManagementPlatform.tsx |

---

## ⚠️ 需要你手动操作的事项

### 1. 在 Supabase 中创建图片存储桶（必须）

图片上传功能需要在 Supabase Storage 中创建存储桶才能正常工作：

1. 登录 Supabase 控制台 → 进入你的项目
2. 左侧菜单 → **Storage**
3. 点击 **New Bucket**，名称填：`material-images`
4. **Public Bucket** 开关设为 **开启**
5. 点击 **Create Bucket**

### 2. 确认 requisitions 表有 image_url 列（建议检查）

申购图片上传后存入 `requisitions` 表的 `image_url` 字段。如果表中没有这个列，需要执行：

```sql
ALTER TABLE requisitions ADD COLUMN IF NOT EXISTS image_url TEXT;
```

---

## 📝 修复详情

### 问题 1：审批通过时多规格库存扣减 ✅
- **改动**：从 `approval.itemName` 中解析规格标签（如 `(70g)`），匹配物品的 `sizes` 数组找到对应的 `sizeId`，传给 `requestMaterialOut`
- **效果**：审批 A4打印纸(70g) 的领用申请时，70g 规格库存正确扣减

### 问题 2：物品上架图片上传 ✅
- **改动**：
  - 新增 `imageUpload.ts` 工具函数（自动压缩图片 + 上传 Supabase Storage）
  - `ItemUpload.tsx` 在提交时先上传图片获取 URL，再传入 `addMaterial()`
- **效果**：上架物品时图片自动上传到 Supabase Storage 并保存 URL

### 问题 3：物品申购图片上传 ✅
- **改动**：`ItemPurchase.tsx` 提交时先上传图片到 Supabase Storage，获取 URL 存入 `requisitions.image_url`
- **效果**：申购单带图片，审批时可以看到物品图片

### 问题 4：删除合并物品 ✅
- **改动**：合并物品时记录所有 `sourceIds`，删除时遍历所有源 ID 逐个删除
- **效果**：删除同名合并物品时，所有关联记录都被清除

### 问题 5/6：日期硬编码 ✅
- **改动**：改为 `new Date().toLocaleDateString()` 动态获取
- **效果**：页面始终显示当天真实日期

### 问题 7：批量审批功能 ✅
- **改动**：添加复选框列、全选、批量批准/驳回按钮
- **效果**：管理员可同时选中多条待审核申请，一键批量批准或驳回

### 问题 8：分页功能 ✅
- **改动**：实现真正的分页逻辑（每页10条），上/下页按钮可用
- **效果**：数据量大时不再一次性全部渲染

### 问题 9：必填项检查 ✅
- **改动**：移除不存在的 `stockPlatform` 字段检查
- **效果**：表单完成度能正确达到 100%

### 问题 10：最近上架列表 ✅
- **改动**：从 `allMaterials` 取最近上架的前5条真实数据
- **效果**：右侧显示实际的上架记录

### 问题 11：搜索语法 ✅
- **改动**：`.ilike('name', ...).or('category.ilike...')` 改为 `.or('name.ilike...%,category.ilike...%')`
- **效果**：搜索更稳定，兼容性更好

### 问题 12：filter 方法 ✅
- **改动**：`.filter('id', 'eq', id)` 改为 `.eq('id', id)`（update 和 delete 都改了）
- **效果**：使用推荐的 Supabase API，兼容性更好

### 问题 13：领用排行数据源 ✅
- **改动**：Dashboard 和 ManagementPlatform 的排行数据从 `requisitions` 表改为 `application_records` 表
- **效果**：领用排行能正确显示已批准的日常领用数据
