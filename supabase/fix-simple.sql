-- =====================================================
-- 简单修复:直接禁用 profiles 表的 RLS(临时方案)
-- =====================================================

-- 临时禁用 profiles 表的 RLS
ALTER TABLE profiles DISABLE ROW LEVEL SECURITY;

-- =====================================================
-- 说明:
-- 禁用 RLS 后,所有经过认证的用户都可以访问 profiles 表
-- 生产环境中应该使用更精细的策略
-- =====================================================
